/* import http from "http";


 //( database / authentication ko lagi)
const server = http.createServer((req, res) =>{
//    res.writeHead(200);

//res.end("backend is working")

/*   (if/else method)
    if(req.url === "/others"){
        res.end("Book");
        res.writeHead(200);
    res.end("backend working")
    }
    else if (req.url === "/books"){


    res.writeHead(200);
    res.end("backend is working")
    }

    else{
        res.writeHead(200);
        res.end("backend is wnice")
    }


*/ 
/*
switch(req.url){
      case "/books":
        if(req.method == "GET"){
              console.log("hello")
        }
        if (req.method == "POST"){

        }
        console.log(req.method);
        res.writeHead(200);
        res.end("backend working");
        break;
        case "/others":
        res.writeHead(200);
        res.end("backend");
        break;
        default:
        res.writeHead(404);
        res.end("backend is not working");
   





}

});

server.listen(8000, ()=>{ 
});


*/